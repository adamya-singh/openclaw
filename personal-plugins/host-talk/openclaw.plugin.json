{
  "id": "host-talk",
  "name": "Host Talk",
  "description": "Hey-openclaw wake word and live voice conversations on the Gateway host's own microphone and speakers.",
  "activation": { "onStartup": true },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "agentId": {
        "type": "string",
        "description": "Agent that owns the voice session. Default \"main\"."
      },
      "wakePhrases": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Wake phrases for this host's microphone. Default [\"hey openclaw\"]. Invented words are matched fuzzily; avoid everyday words."
      }
    }
  }
}
